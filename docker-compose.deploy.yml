# Hybrid Vision System — deploy / quickstart compose file
#
# Used by g1-install.sh. Contains only data mounts; app code is baked into the images.
# For development (live-editing app.py / static / kpis), use docker-compose.yml instead.
#
# Setup:
#   cp .env.example .env        # then fill in your two paths
#   docker compose -f docker-compose.deploy.yml up -d

services:

  web:
    image: gitlab.lrz.de:5005/ldv/teaching/ami/ami2026/group01/ami-web:latest
    ports:
      - "8080:8080"
    volumes:
      - ${FRED_DATA_PATH}:/data/fred:ro
      - ${RECON_DATA_PATH}:/data/recon
      - ./static:/app/static:ro
      - ./kpis:/app/kpis:ro
    environment:
      - E2VID_URL=http://e2vid:8001
      - HYPERE2VID_URL=http://hypere2vid:8002
      - FUSION_URL=http://fusion:8003
    depends_on:
      e2vid:
        condition: service_healthy

  e2vid:
    image: gitlab.lrz.de:5005/ldv/teaching/ami/ami2026/group01/ami-e2vid:latest
    expose:
      - "8001"
    volumes:
      - ${FRED_DATA_PATH}:/data/fred:ro
      - ${RECON_DATA_PATH}:/data/recon
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  hypere2vid:
    image: gitlab.lrz.de:5005/ldv/teaching/ami/ami2026/group01/ami-hypere2vid:latest
    expose:
      - "8002"
    volumes:
      - ${FRED_DATA_PATH}:/data/fred:ro
      - ${RECON_DATA_PATH}:/data/recon
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  fusion:
    image: gitlab.lrz.de:5005/ldv/teaching/ami/ami2026/group01/ami-fusion:latest
    expose:
      - "8003"
    volumes:
      - ${FRED_DATA_PATH}:/data/fred:ro
      - ${RECON_DATA_PATH}:/data/recon
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8003/health"]
      interval: 10s
      timeout: 5s
      retries: 5
